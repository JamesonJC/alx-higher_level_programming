#!/usr/bin/python3

def search_replace(my_list, search, replace):
    if my_list:
        for idx in my_list:
            if idx == my_list[search]:
                my_list[elemen] = replace
    return (my_list)
